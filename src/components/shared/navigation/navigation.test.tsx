import { render, screen } from '@/test-utils';

describe('Welcome component', () => {
  it('should render correctly', () => {
    // This is an empty test for now
    expect(true).toBe(true);
  });
});
